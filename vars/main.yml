---
deploy_drupal_profiles_base_folder: "{{ (app_drupal_version|default('8')|version_compare('8', '=='))|ternary('/core/profiles', '/profiles') }}"
deploy_php_version_default: "{{ (ansible_distribution_release == 'xenial')|ternary('7.0', '5' ) }}"