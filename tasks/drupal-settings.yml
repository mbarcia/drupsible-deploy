---
- name: Copy templated settings.php
  template:
    src: "settings.php.j2"
    dest: "{{ codebase_dir }}/sites/default/settings.php"
    owner: "{{ app_env['user'] }}"
    group: "{{ app_env['group'] }}"
    mode: 444
