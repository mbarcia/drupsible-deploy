---
- name: Copy and decompress files tarball
  unarchive:
    src: "files-tarballs/{{ app_env['files_tarball_filename'] }}"
    dest: "{{ codebase_dir }}/sites/default/"
    mode: "g+w"
    group: "{{ app_env['group'] }}"
  become: yes
